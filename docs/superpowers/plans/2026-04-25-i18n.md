# i18n (en + ja) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PDFPW UI に英日 2 言語対応を導入する。URL prefix (`/en/` `/ja/`) ベース、paraglide-js + Vite 経由のメッセージカタログ、ランディングは locale 別 HTML を pre-render する。

**Architecture:** TanStack Router を `$locale` 配下に再構成し、URL の locale segment を paraglide runtime と同期する。既存 ~195 個のハードコード文字列を `m.foo()` 関数呼び出しに置換し、`messages/{en,ja}.json` で型安全に管理。Vite plugin がビルド時に locale 別の `<head>` を持つ `en/index.html` `ja/index.html` を生成し、SEO 用の `hreflang` `og:locale` 等を設定。

**Tech Stack:** `@inlang/paraglide-js` v2, TanStack Router (file-based), React 19, Vite 7, vitest + jsdom + @testing-library/react, Biome, pnpm。

設計仕様: `docs/superpowers/specs/2026-04-25-i18n-design.md`

---

## File Structure (このプランで触る主要ファイル)

**新規作成**
- `project.inlang/settings.json` — paraglide プロジェクト設定
- `messages/en.json` `messages/ja.json` — メッセージカタログ
- `src/lib/format.ts` + `src/lib/format.test.ts` — 日付/数値フォーマッタ
- `src/lib/i18n.test.ts` — メッセージカタログ整合性テスト
- `src/components/LocaleSwitcher.tsx` + `src/components/LocaleSwitcher.test.tsx`
- `src/routes/index.tsx` — ルート `/` redirect
- `src/routes/index.test.tsx` — redirect ロジックテスト
- `src/routes/$locale/route.tsx` — locale 検証 + paraglide 同期
- `src/vite-plugins/locale-html-plugin.ts` — multi-HTML 生成 plugin
- `index.template.html` — locale 別 HTML テンプレート

**移動 (path 変更のみ)**
- `src/routes/(main)/` → `src/routes/$locale/(main)/`
- `src/routes/presentation/` → `src/routes/$locale/presentation/`

**修正**
- `src/lib/keybindings.ts` — label/hint を message 関数参照に
- `src/components/Header.tsx` — LocaleSwitcher 配置
- 各 UI コンポーネント (~19 ファイル) — 文字列を `m.foo()` 呼び出しに置換
- `index.html` — 検出 redirect ページに刷新
- `vite.config.ts` — paraglide plugin と localeHtmlPlugin 登録、multi-input 設定
- `package.json` — paraglide 依存追加、postinstall script

**生成 (gitignore)**
- `src/paraglide/` — paraglide compile output

---

## Task 1: paraglide-js パッケージ追加と設定

**Files:**
- Create: `project.inlang/settings.json`
- Create: `messages/en.json`
- Create: `messages/ja.json`
- Modify: `.gitignore`
- Modify: `package.json` (依存追加)

- [ ] **Step 1: paraglide-js 依存追加**

```bash
pnpm add -D @inlang/paraglide-js
```

- [ ] **Step 2: `.gitignore` に paraglide 生成ディレクトリを追加**

`.gitignore` の末尾に以下を追加:

```
# paraglide-js generated
src/paraglide/
```

- [ ] **Step 3: `project.inlang/settings.json` を作成**

```json
{
  "$schema": "https://inlang.com/schema/project-settings",
  "baseLocale": "en",
  "locales": ["en", "ja"],
  "modules": [
    "https://cdn.jsdelivr.net/npm/@inlang/plugin-message-format@4/dist/index.js"
  ],
  "plugin.inlang.messageFormat": {
    "pathPattern": "./messages/{locale}.json"
  }
}
```

- [ ] **Step 4: `messages/en.json` を最小内容で作成 (smoke test 用)**

```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "smoke_test": "ok"
}
```

- [ ] **Step 5: `messages/ja.json` を最小内容で作成**

```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "smoke_test": "OK"
}
```

- [ ] **Step 6: paraglide compile を手動実行して動作確認**

```bash
pnpm exec paraglide-js compile --project ./project.inlang --outdir ./src/paraglide
```

Expected: `src/paraglide/messages.js`, `src/paraglide/runtime.js` が生成される。

- [ ] **Step 7: `src/paraglide/runtime.js` の `setLocale`/`getLocale`/`locales` export を確認**

```bash
grep -E "(setLocale|getLocale|locales)" src/paraglide/runtime.js | head -10
```

Expected: それぞれの export が見つかる。

- [ ] **Step 8: コミット**

```bash
git add .gitignore project.inlang/ messages/ package.json pnpm-lock.yaml
git commit -m "feat(i18n): paraglide-js セットアップとプロジェクト構成

@inlang/paraglide-js v2 を導入し、project.inlang/ と messages/{en,ja}.json
を最小内容で作成。生成ディレクトリ src/paraglide/ は gitignore。"
```

---

## Task 2: Vite plugin 統合

**Files:**
- Modify: `vite.config.ts`

- [ ] **Step 1: `vite.config.ts` に paraglide plugin の import を追加**

`vite.config.ts` 上部の import 群に追加 (UnpluginTypia 等の隣):

```ts
import { paraglideVitePlugin } from "@inlang/paraglide-js";
```

- [ ] **Step 2: `plugins: [...]` 配列の最初 (UnpluginTypia の前) に paraglide plugin を追加**

```ts
plugins: [
  paraglideVitePlugin({
    project: "./project.inlang",
    outdir: "./src/paraglide",
    strategy: ["url", "localStorage", "preferredLanguage", "baseLocale"],
  }),
  UnpluginTypia(),
  // ... 既存の plugins
],
```

- [ ] **Step 3: dev server を起動して plugin が compile を走らせるか確認**

```bash
pnpm dev
```

Expected: ターミナルにエラーが出ず、`src/paraglide/` が再生成される。`Ctrl-C` で停止。

- [ ] **Step 4: tsc がパスすることを確認**

```bash
pnpm tsc -b
```

Expected: エラーなし。

- [ ] **Step 5: コミット**

```bash
git add vite.config.ts
git commit -m "feat(i18n): paraglide Vite plugin を登録

paraglideVitePlugin を vite.config.ts に追加。strategy に url を含めることで
URL ベースの locale 検出を有効化。"
```

---

## Task 3: 日付・数値フォーマッタ

**Files:**
- Create: `src/lib/format.ts`
- Create: `src/lib/format.test.ts`

- [ ] **Step 1: テストを先に書く (失敗する状態で)**

`src/lib/format.test.ts`:

```ts
// @vitest-environment jsdom
import { setLocale } from "#src/paraglide/runtime.js";
import { describe, expect, it } from "vitest";
import { formatDateTime, formatNumber } from "./format.ts";

describe("formatDateTime", () => {
	it("locale=en で英語形式の日付を返す", () => {
		setLocale("en", { reload: false });
		const result = formatDateTime(new Date("2026-04-25T10:30:00Z"), {
			dateStyle: "medium",
			timeStyle: "short",
			timeZone: "UTC",
		});
		expect(result).toMatch(/Apr.*2026/);
	});

	it("locale=ja で日本語形式の日付を返す", () => {
		setLocale("ja", { reload: false });
		const result = formatDateTime(new Date("2026-04-25T10:30:00Z"), {
			dateStyle: "medium",
			timeStyle: "short",
			timeZone: "UTC",
		});
		expect(result).toMatch(/2026/);
		expect(result).toMatch(/4月|04/);
	});
});

describe("formatNumber", () => {
	it("locale=en で 1,234 形式", () => {
		setLocale("en", { reload: false });
		expect(formatNumber(1234)).toBe("1,234");
	});

	it("locale=ja で 1,234 形式 (日本語も同形式)", () => {
		setLocale("ja", { reload: false });
		expect(formatNumber(1234)).toBe("1,234");
	});
});
```

- [ ] **Step 2: テスト実行 (失敗を確認)**

```bash
pnpm test src/lib/format.test.ts
```

Expected: FAIL — `./format.ts` が存在しないため import エラー。

- [ ] **Step 3: 実装**

`src/lib/format.ts`:

```ts
import { getLocale } from "#src/paraglide/runtime.js";

export function formatDateTime(
	date: Date | number,
	options?: Intl.DateTimeFormatOptions,
): string {
	return new Intl.DateTimeFormat(getLocale(), options).format(date);
}

export function formatNumber(
	n: number,
	options?: Intl.NumberFormatOptions,
): string {
	return new Intl.NumberFormat(getLocale(), options).format(n);
}
```

- [ ] **Step 4: テストパス確認**

```bash
pnpm test src/lib/format.test.ts
```

Expected: 4 tests passed.

- [ ] **Step 5: コミット**

```bash
git add src/lib/format.ts src/lib/format.test.ts
git commit -m "feat(i18n): 日付・数値フォーマッタを paraglide locale 連動に

Intl.DateTimeFormat と Intl.NumberFormat を getLocale() で動的に locale 指定する
薄ラッパを追加。既存の toLocaleString(\"ja-JP\") 固定箇所をこれで置き換える準備。"
```

---

## Task 4: Routes を `$locale/` 配下に移動

**Files:**
- Move: `src/routes/(main)/` → `src/routes/$locale/(main)/`
- Move: `src/routes/presentation/` → `src/routes/$locale/presentation/`

- [ ] **Step 1: `$locale` ディレクトリ作成と既存ルート移動**

```bash
mkdir -p src/routes/\$locale
git mv src/routes/\(main\) src/routes/\$locale/\(main\)
git mv src/routes/presentation src/routes/\$locale/presentation
```

- [ ] **Step 2: 移動後の構造確認**

```bash
ls src/routes/
ls src/routes/\$locale/
ls src/routes/\$locale/\(main\)/
ls src/routes/\$locale/presentation/
```

Expected:
- `src/routes/`: `__root.tsx`, `-hooks/`, `$locale/`
- `src/routes/$locale/`: `(main)/`, `presentation/`
- `(main)/`: 元の `index.tsx`, `presenter.tsx`, `licenses.tsx`, `route.tsx`, `-presenter/`, `-index/`
- `presentation/`: 元の `index.tsx`, `route.tsx`, `-Menu.tsx`, `-SlideStage.tsx`, `-hooks/`

- [ ] **Step 3: 既存ルートファイルの `createFileRoute` パスを `$locale/` 配下に変更**

`src/routes/$locale/(main)/route.tsx`:
```ts
export const Route = createFileRoute("/$locale/(main)")({
```

`src/routes/$locale/(main)/index.tsx`:
```ts
export const Route = createFileRoute("/$locale/(main)/")({
```

`src/routes/$locale/(main)/licenses.tsx`:
```ts
export const Route = createFileRoute("/$locale/(main)/licenses")({
```

`src/routes/$locale/(main)/presenter.tsx`:
```ts
export const Route = createFileRoute("/$locale/(main)/presenter")({
```

`src/routes/$locale/presentation/route.tsx`:
```ts
export const Route = createFileRoute("/$locale/presentation")({
```

`src/routes/$locale/presentation/index.tsx`:
```ts
export const Route = createFileRoute("/$locale/presentation/")({
```

- [ ] **Step 4: dev server 起動して TanStack Router plugin が `routeTree.gen.ts` を再生成することを確認**

```bash
pnpm dev
```

- [ ] **Step 5: routeTree が `/$locale/` を含むことを確認**

```bash
grep -E '\\$locale' src/routeTree.gen.ts | head -10
```

Expected: `$locale` を含むルート定義が生成されている。`Ctrl-C` で dev 停止。

- [ ] **Step 6: 既存 `Link to=` / `router.navigate` / `router.buildLocation` を含むファイルを列挙**

```bash
grep -rn 'to="/' src/routes/ src/components/ src/hooks/ 2>/dev/null
```

これは次の Task 5 〜 7 で順次対応。今は**まだ動かない状態**でコミットせず、Task 5 へ進む。

---

## Task 5: `$locale/route.tsx` に locale 検証と paraglide 同期を実装

**Files:**
- Create: `src/routes/$locale/route.tsx`

- [ ] **Step 1: `src/routes/$locale/route.tsx` を作成**

```tsx
import { createFileRoute, Outlet, redirect } from "@tanstack/react-router";
import { useEffect } from "react";
import { type Locale, locales, setLocale } from "#src/paraglide/runtime.js";

function isValidLocale(value: string): value is Locale {
	return (locales as readonly string[]).includes(value);
}

export const Route = createFileRoute("/$locale")({
	beforeLoad: ({ params }) => {
		if (!isValidLocale(params.locale)) {
			throw redirect({
				to: "/$locale",
				params: { locale: "en" },
				replace: true,
			});
		}
	},
	component: LocaleLayout,
});

function LocaleLayout() {
	const { locale } = Route.useParams();

	useEffect(() => {
		setLocale(locale as Locale, { reload: false });
	}, [locale]);

	if (typeof document !== "undefined") {
		document.documentElement.lang = locale;
	}

	return <Outlet />;
}
```

- [ ] **Step 2: paraglide runtime からの import 名を確認**

```bash
grep -E "^export (const|function) " src/paraglide/runtime.js | head -20
```

Expected: `setLocale`, `getLocale`, `locales` (and `Locale` type) が export されている。
もし export 名が違う場合 (例: `setLanguageTag`)、最新 paraglide v2 docs に従って Step 1 の import 行と呼び出しを調整。

- [ ] **Step 3: tsc 確認**

```bash
pnpm tsc -b
```

paraglide 生成ファイルから `Locale` 型が見つからない場合、`type Locale = "en" | "ja"` を `src/routes/$locale/route.tsx` 内に定義してフォールバック。

- [ ] **Step 4: Task 4 のルート移動と合わせてコミット**

`git mv` による移動 + 各 `createFileRoute` パスの更新 + 新規 `$locale/route.tsx` を 1 コミットにまとめる:

```bash
git add -A src/routes/
git status  # 確認: $locale 配下への移動、route.tsx 追加が見える
git commit -m "feat(i18n): ルートを \$locale 配下に再構成

(main)/ と presentation/ を \$locale/ 配下に移動し、各 createFileRoute パスを
更新。\$locale/route.tsx で locale 検証 (不正値は /en/ にリダイレクト) と
paraglide setLocale() による URL 同期、document.documentElement.lang 更新を行う。"
```

---

## Task 6: ルート `/` の検出 redirect ロジック

**Files:**
- Create: `src/routes/index.tsx`
- Create: `src/routes/index.test.tsx`

- [ ] **Step 1: テストを先に書く**

`src/routes/index.test.tsx`:

```tsx
// @vitest-environment jsdom
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
import { detectInitialLocale } from "./index.tsx";

describe("detectInitialLocale", () => {
	beforeEach(() => {
		localStorage.clear();
	});
	afterEach(() => {
		localStorage.clear();
		vi.restoreAllMocks();
	});

	it("localStorage の有効値を最優先する", () => {
		localStorage.setItem("pdfpw:locale", "ja");
		vi.spyOn(navigator, "language", "get").mockReturnValue("en-US");
		expect(detectInitialLocale()).toBe("ja");
	});

	it("localStorage が無効値なら navigator.language にフォールバック", () => {
		localStorage.setItem("pdfpw:locale", "fr");
		vi.spyOn(navigator, "language", "get").mockReturnValue("ja-JP");
		expect(detectInitialLocale()).toBe("ja");
	});

	it("navigator.language が ja で始まれば ja", () => {
		vi.spyOn(navigator, "language", "get").mockReturnValue("ja");
		expect(detectInitialLocale()).toBe("ja");
	});

	it("どれにも該当しなければ en", () => {
		vi.spyOn(navigator, "language", "get").mockReturnValue("de-DE");
		expect(detectInitialLocale()).toBe("en");
	});

	it("localStorage が \"en\" なら navigator.language が ja でも en", () => {
		localStorage.setItem("pdfpw:locale", "en");
		vi.spyOn(navigator, "language", "get").mockReturnValue("ja-JP");
		expect(detectInitialLocale()).toBe("en");
	});
});
```

- [ ] **Step 2: テスト実行 (失敗を確認)**

```bash
pnpm test src/routes/index.test.tsx
```

Expected: FAIL — `./index.tsx` が無い、または `detectInitialLocale` が export されていない。

- [ ] **Step 3: 実装**

`src/routes/index.tsx`:

```tsx
import { createFileRoute, Navigate } from "@tanstack/react-router";

export type Locale = "en" | "ja";

export function detectInitialLocale(): Locale {
	try {
		const stored = localStorage.getItem("pdfpw:locale");
		if (stored === "en" || stored === "ja") return stored;
	} catch {
		// localStorage が利用不可な環境では無視
	}
	const lang = typeof navigator !== "undefined" ? navigator.language : "";
	return lang.toLowerCase().startsWith("ja") ? "ja" : "en";
}

export const Route = createFileRoute("/")({
	component: RootRedirect,
});

function RootRedirect() {
	const locale = detectInitialLocale();
	return <Navigate to="/$locale" params={{ locale }} replace />;
}
```

- [ ] **Step 4: テストパス確認**

```bash
pnpm test src/routes/index.test.tsx
```

Expected: 5 tests passed.

- [ ] **Step 5: コミット**

```bash
git add src/routes/index.tsx src/routes/index.test.tsx
git commit -m "feat(i18n): ルート / で locale を検出してリダイレクト

localStorage > navigator.language > en の優先順で初期 locale を決定し、
TanStack Router の Navigate で /\$locale/ に replace する。"
```

---

## Task 7: `Link to=` / `router.navigate` の locale 化

**Files:**
- Modify: `src/components/Header.tsx`
- Modify: `src/routes/$locale/(main)/index.tsx`
- Modify: `src/routes/$locale/(main)/presenter.tsx`
- Modify: `src/routes/$locale/presentation/index.tsx`

- [ ] **Step 1: Header の `<Link to="/">` を `$locale` 対応に**

`src/components/Header.tsx` の Link を以下に置き換え:

```tsx
import { Link, useParams } from "@tanstack/react-router";

// ... GithubMark など既存定義はそのまま ...

export default function Header({ onHelpClick }: HeaderProps = {}) {
	const { locale } = useParams({ strict: false }) as { locale?: string };
	const safeLocale = locale === "ja" || locale === "en" ? locale : "en";

	return (
		<header className="flex items-center justify-between border-b border-border bg-bg px-6 py-3">
			<Link
				to="/$locale"
				params={{ locale: safeLocale }}
				className="flex items-center gap-2 text-fg transition-opacity hover:opacity-80"
			>
				{/* ... 既存 children ... */}
			</Link>
			<div className="flex items-center gap-1">
				{/* ... 既存 children だが Licenses link を以下に置換 */}
				<Link
					to="/$locale/licenses"
					params={{ locale: safeLocale }}
					className="inline-flex h-8 w-8 items-center justify-center rounded-md text-muted transition-colors hover:bg-surface hover:text-fg"
					aria-label="Licenses"
				>
					<FileTextIcon className="size-4" />
				</Link>
				<ThemeToggle />
			</div>
		</header>
	);
}
```

- [ ] **Step 2: `(main)/index.tsx` の `router.navigate` / `router.buildLocation` を locale 化**

`src/routes/$locale/(main)/index.tsx` 内:
- `await router.navigate({ to: "/presenter", search: { file: pdf.name }, ... })` を以下に変更:

```tsx
const { locale } = Route.useParams();
// ...
await router.navigate({
	to: "/$locale/presenter",
	params: { locale },
	search: { file: pdf.name },
	state: { pdf: pdfHandle ?? pdf, pdfpc: pdfpcHandle ?? pdfpc },
});
const url = router.buildLocation({
	to: "/$locale/presentation",
	params: { locale },
	search: { file: pdf.name },
}).href;
```

(つまり `Route.useParams()` で locale を取り、各 router 呼び出しで `params: { locale }` を渡す)

- [ ] **Step 3: `(main)/presenter.tsx` の "ホームへ戻る" Link を locale 化**

`src/routes/$locale/(main)/presenter.tsx` の `MissingFileScreen` 内で:

```tsx
import { Route as PresenterRoute } from "./presenter";
// ↑実際には createFileRoute から useParams を使う:
// 既存: <Link to="/">ホームへ戻る</Link>
// →
function MissingFileScreen({ title }: { title: string }) {
	const { locale } = Route.useParams();
	return (
		<main className="min-h-screen bg-background text-foreground">
			<div className="mx-auto flex max-w-2xl flex-col gap-4 px-6 py-12">
				<h1 className="text-xl font-semibold">{title}</h1>
				<p className="text-muted-foreground">
					ホームに戻って再度ファイルを選択してください。
				</p>
				<Button asChild className="w-fit">
					<Link to="/$locale" params={{ locale }}>ホームへ戻る</Link>
				</Button>
			</div>
		</main>
	);
}
```

注意: `MissingFileScreen` は `Route.useParams()` をトップで使う必要があるため、`RouteComponent` 経由で `locale` prop として渡すパターンに変更してもよい。簡潔さ優先で `Route.useParams()` 直接使用が望ましい。

- [ ] **Step 4: `presentation/index.tsx` の "ホームへ戻る" Link を locale 化**

`src/routes/$locale/presentation/index.tsx` の `RouteComponent` 内 missing file branch で同様に:

```tsx
const { locale } = Route.useParams();
// ...
<Link to="/$locale" params={{ locale }}>ホームへ戻る</Link>
```

- [ ] **Step 5: `presentation/index.tsx` 等で BroadcastChannel 経由のものは URL に locale 不要**

BroadcastChannel API は URL に依存しないので変更不要。確認のみ。

- [ ] **Step 6: tsc / dev 確認**

```bash
pnpm tsc -b
pnpm dev
```

Browser で `http://localhost:6123/` にアクセス → `/en` または `/ja` にリダイレクトされ、ホームが表示されることを確認。`Ctrl-C` で停止。

- [ ] **Step 7: コミット**

```bash
git add src/components/Header.tsx \
        src/routes/\$locale/\(main\)/index.tsx \
        src/routes/\$locale/\(main\)/presenter.tsx \
        src/routes/\$locale/presentation/index.tsx
git commit -m "feat(i18n): 内部リンクと navigation を \$locale 対応に

Header の Link、ホーム画面の router.navigate、presenter/presentation の
\"ホームへ戻る\" Link 等を全て \$locale param 経由で構築するよう変更。"
```

---

## Task 8: LocaleSwitcher コンポーネント

**Files:**
- Create: `src/components/LocaleSwitcher.tsx`
- Create: `src/components/LocaleSwitcher.test.tsx`

- [ ] **Step 1: テストを先に書く**

`src/components/LocaleSwitcher.test.tsx`:

```tsx
// @vitest-environment jsdom
import {
	createMemoryHistory,
	createRootRoute,
	createRoute,
	createRouter,
	Outlet,
	RouterProvider,
} from "@tanstack/react-router";
import { fireEvent, render, screen } from "@testing-library/react";
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import { LocaleSwitcher } from "./LocaleSwitcher.tsx";

function setupRouter(initialPath: string) {
	const rootRoute = createRootRoute({ component: () => <Outlet /> });
	const localeRoute = createRoute({
		getParentRoute: () => rootRoute,
		path: "/$locale",
		component: () => (
			<>
				<LocaleSwitcher />
				<Outlet />
			</>
		),
	});
	const indexRoute = createRoute({
		getParentRoute: () => localeRoute,
		path: "/",
		component: () => <div>home</div>,
	});
	const router = createRouter({
		routeTree: rootRoute.addChildren([localeRoute.addChildren([indexRoute])]),
		history: createMemoryHistory({ initialEntries: [initialPath] }),
	});
	return router;
}

describe("LocaleSwitcher", () => {
	beforeEach(() => localStorage.clear());
	afterEach(() => localStorage.clear());

	it("現在 locale ボタンに aria-pressed='true' が付く", async () => {
		const router = setupRouter("/en");
		render(<RouterProvider router={router} />);
		await screen.findByRole("button", { name: /english/i });
		const enBtn = screen.getByRole("button", { name: /english/i });
		const jaBtn = screen.getByRole("button", { name: /japanese|日本語/i });
		expect(enBtn).toHaveAttribute("aria-pressed", "true");
		expect(jaBtn).toHaveAttribute("aria-pressed", "false");
	});

	it("ja ボタンクリックで /ja に navigate し localStorage が更新される", async () => {
		const router = setupRouter("/en");
		render(<RouterProvider router={router} />);
		const jaBtn = await screen.findByRole("button", {
			name: /japanese|日本語/i,
		});
		fireEvent.click(jaBtn);
		await router.invalidate();
		expect(router.state.location.pathname.startsWith("/ja")).toBe(true);
		expect(localStorage.getItem("pdfpw:locale")).toBe("ja");
	});
});
```

- [ ] **Step 2: テスト実行 (失敗を確認)**

```bash
pnpm test src/components/LocaleSwitcher.test.tsx
```

Expected: FAIL — `./LocaleSwitcher.tsx` が存在しない。

- [ ] **Step 3: 実装**

`src/components/LocaleSwitcher.tsx`:

```tsx
import { useLocation, useNavigate, useParams } from "@tanstack/react-router";
import { cn } from "#src/lib/utils.ts";

const LOCALES = [
	{ code: "en", short: "EN", name: "English" },
	{ code: "ja", short: "JA", name: "日本語" },
] as const;

type LocaleCode = (typeof LOCALES)[number]["code"];

const LOCALE_STORAGE_KEY = "pdfpw:locale";

function rewritePath(pathname: string, nextLocale: LocaleCode): string {
	const segments = pathname.split("/").filter(Boolean);
	if (segments.length === 0) return `/${nextLocale}`;
	if (segments[0] === "en" || segments[0] === "ja") {
		segments[0] = nextLocale;
	} else {
		segments.unshift(nextLocale);
	}
	return `/${segments.join("/")}`;
}

export function LocaleSwitcher() {
	const navigate = useNavigate();
	const location = useLocation();
	const params = useParams({ strict: false }) as { locale?: string };
	const current: LocaleCode = params.locale === "ja" ? "ja" : "en";

	function switchTo(next: LocaleCode) {
		if (next === current) return;
		try {
			localStorage.setItem(LOCALE_STORAGE_KEY, next);
		} catch {
			// localStorage 利用不可環境では握り潰し
		}
		const nextPath = rewritePath(location.pathname, next);
		navigate({ to: nextPath, replace: true });
	}

	return (
		<div
			role="group"
			aria-label="Switch language"
			className="inline-flex items-center rounded-md border border-border bg-bg p-0.5"
		>
			{LOCALES.map((l) => {
				const active = l.code === current;
				return (
					<button
						key={l.code}
						type="button"
						onClick={() => switchTo(l.code)}
						aria-pressed={active}
						aria-label={l.name}
						className={cn(
							"inline-flex h-7 min-w-[28px] items-center justify-center rounded-sm px-2 font-mono text-[10px] font-semibold transition-colors",
							active
								? "bg-accent-soft text-accent"
								: "text-muted hover:bg-surface hover:text-fg",
						)}
					>
						{l.short}
					</button>
				);
			})}
		</div>
	);
}
```

- [ ] **Step 4: テストパス確認**

```bash
pnpm test src/components/LocaleSwitcher.test.tsx
```

Expected: 2 tests passed.

- [ ] **Step 5: コミット**

```bash
git add src/components/LocaleSwitcher.tsx src/components/LocaleSwitcher.test.tsx
git commit -m "feat(i18n): LocaleSwitcher コンポーネント追加

EN/JA のセグメントトグルで現在 URL の locale prefix を入れ替え、
localStorage にも保存。aria-pressed で現在 locale を示す。"
```

---

## Task 9: Header に LocaleSwitcher を組み込み

**Files:**
- Modify: `src/components/Header.tsx`

- [ ] **Step 1: Header に LocaleSwitcher を配置**

`src/components/Header.tsx` の右側コントロール群、`onHelpClick` ボタンの**直前**に挿入:

```tsx
import { LocaleSwitcher } from "./LocaleSwitcher.tsx";
// ...
<div className="flex items-center gap-1">
	<LocaleSwitcher />
	{onHelpClick && (
		<button
			// ...既存
		/>
	)}
	{/* ... 残り既存 */}
</div>
```

- [ ] **Step 2: dev で動作確認**

```bash
pnpm dev
```

Browser で `/en` と `/ja` を切り替えてホーム → presenter (PDF を読み込んで遷移) で URL が正しく付け替わることを確認。`Ctrl-C` で停止。

- [ ] **Step 3: tsc & lint 確認**

```bash
pnpm tsc -b
pnpm lint
```

- [ ] **Step 4: コミット**

```bash
git add src/components/Header.tsx
git commit -m "feat(i18n): Header に LocaleSwitcher を配置"
```

---

## Task 10: KEYBINDING_CATALOG を message 関数参照化

**Files:**
- Modify: `src/lib/keybindings.ts`
- Modify: `src/components/KeybindingHelpDialog.tsx`
- Modify: `messages/en.json`
- Modify: `messages/ja.json`

- [ ] **Step 1: messages に keybinding 関連のキーを追加**

`messages/en.json` にマージ (既存 `smoke_test` は残す):

```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "smoke_test": "ok",
  "kb_slide_next_label": "Next slide",
  "kb_slide_prev_label": "Previous slide",
  "kb_slide_next_user_label": "Next slide group",
  "kb_slide_prev_user_label": "Previous slide group",
  "kb_slide_next_10_label": "Skip 10 forward",
  "kb_slide_prev_10_label": "Skip 10 backward",
  "kb_slide_first_label": "First slide",
  "kb_slide_last_label": "Last slide",
  "kb_slide_history_back_label": "Navigate back in history",
  "kb_slide_jump_mode_label": "Jump to slide N",
  "kb_slide_jump_mode_hint": "Then type digits and press Enter",
  "kb_tool_laser_label": "Toggle laser pointer",
  "kb_tool_pen_label": "Toggle pen",
  "kb_tool_erase_label": "Erase pen drawings",
  "kb_tool_exit_label": "Exit tool / close dialog",
  "kb_view_overview_label": "Toggle overview",
  "kb_view_fullscreen_label": "Toggle fullscreen",
  "kb_system_reset_timer_label": "Reset timer",
  "kb_system_help_label": "Show keyboard help",
  "kb_category_navigation": "Navigation",
  "kb_category_tools": "Tools",
  "kb_category_view": "View",
  "kb_category_system": "System"
}
```

`messages/ja.json` にマージ:

```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "smoke_test": "OK",
  "kb_slide_next_label": "次のスライド",
  "kb_slide_prev_label": "前のスライド",
  "kb_slide_next_user_label": "次のスライドグループ",
  "kb_slide_prev_user_label": "前のスライドグループ",
  "kb_slide_next_10_label": "10 ページ進む",
  "kb_slide_prev_10_label": "10 ページ戻る",
  "kb_slide_first_label": "最初のスライド",
  "kb_slide_last_label": "最後のスライド",
  "kb_slide_history_back_label": "履歴を戻る",
  "kb_slide_jump_mode_label": "指定スライドへジャンプ",
  "kb_slide_jump_mode_hint": "数字を入力して Enter で確定",
  "kb_tool_laser_label": "レーザーポインタ切替",
  "kb_tool_pen_label": "ペン切替",
  "kb_tool_erase_label": "ペン描画を消去",
  "kb_tool_exit_label": "ツール終了 / ダイアログを閉じる",
  "kb_view_overview_label": "一覧表示の切替",
  "kb_view_fullscreen_label": "フルスクリーン切替",
  "kb_system_reset_timer_label": "タイマーをリセット",
  "kb_system_help_label": "キーボードヘルプを表示",
  "kb_category_navigation": "ナビゲーション",
  "kb_category_tools": "ツール",
  "kb_category_view": "表示",
  "kb_category_system": "システム"
}
```

- [ ] **Step 2: paraglide compile を走らせる**

```bash
pnpm exec paraglide-js compile --project ./project.inlang --outdir ./src/paraglide
```

- [ ] **Step 3: `src/lib/keybindings.ts` の `ActionDefinition` 型を関数参照化**

`label: string` → `label: () => string`、`hint?: string` → `hint?: () => string`:

```ts
import {
	kb_slide_next_label,
	kb_slide_prev_label,
	kb_slide_next_user_label,
	kb_slide_prev_user_label,
	kb_slide_next_10_label,
	kb_slide_prev_10_label,
	kb_slide_first_label,
	kb_slide_last_label,
	kb_slide_history_back_label,
	kb_slide_jump_mode_label,
	kb_slide_jump_mode_hint,
	kb_tool_laser_label,
	kb_tool_pen_label,
	kb_tool_erase_label,
	kb_tool_exit_label,
	kb_view_overview_label,
	kb_view_fullscreen_label,
	kb_system_reset_timer_label,
	kb_system_help_label,
} from "#src/paraglide/messages.js";

// ...既存 type 定義はそのまま (ActionId, Scope, Category, Binding)

export interface ActionDefinition {
	readonly category: Category;
	readonly scope: Scope;
	readonly bindings: readonly Binding[];
	readonly label: () => string;
	readonly hint?: () => string;
}

export const KEYBINDING_CATALOG: Record<ActionId, ActionDefinition> = {
	"slide.next-10": {
		category: "navigation",
		scope: "presenter",
		bindings: [
			{ key: "ArrowRight", shift: true },
			{ key: "PageDown", shift: true },
		],
		label: kb_slide_next_10_label,
	},
	"slide.prev-10": {
		category: "navigation",
		scope: "presenter",
		bindings: [
			{ key: "ArrowLeft", shift: true },
			{ key: "PageUp", shift: true },
		],
		label: kb_slide_prev_10_label,
	},
	"slide.next": {
		category: "navigation",
		scope: "both",
		bindings: [{ key: "ArrowRight" }, { key: " " }, { key: "PageDown" }],
		label: kb_slide_next_label,
	},
	"slide.prev": {
		category: "navigation",
		scope: "both",
		bindings: [{ key: "ArrowLeft" }, { key: "PageUp" }],
		label: kb_slide_prev_label,
	},
	"slide.next-user": {
		category: "navigation",
		scope: "presenter",
		bindings: [{ key: "ArrowDown" }],
		label: kb_slide_next_user_label,
	},
	"slide.prev-user": {
		category: "navigation",
		scope: "presenter",
		bindings: [{ key: "ArrowUp" }],
		label: kb_slide_prev_user_label,
	},
	"slide.first": {
		category: "navigation",
		scope: "both",
		bindings: [{ key: "Home" }],
		label: kb_slide_first_label,
	},
	"slide.last": {
		category: "navigation",
		scope: "both",
		bindings: [{ key: "End" }],
		label: kb_slide_last_label,
	},
	"slide.history-back": {
		category: "navigation",
		scope: "presenter",
		bindings: [{ key: "Backspace" }],
		label: kb_slide_history_back_label,
	},
	"slide.jump-mode": {
		category: "navigation",
		scope: "presenter",
		bindings: [{ key: "g" }],
		label: kb_slide_jump_mode_label,
		hint: kb_slide_jump_mode_hint,
	},
	"tool.laser": {
		category: "tools",
		scope: "both",
		bindings: [{ key: "l" }],
		label: kb_tool_laser_label,
	},
	"tool.pen": {
		category: "tools",
		scope: "both",
		bindings: [{ key: "d" }],
		label: kb_tool_pen_label,
	},
	"tool.erase": {
		category: "tools",
		scope: "both",
		bindings: [{ key: "e" }],
		label: kb_tool_erase_label,
	},
	"tool.exit": {
		category: "tools",
		scope: "both",
		bindings: [{ key: "Escape" }],
		label: kb_tool_exit_label,
	},
	"view.overview": {
		category: "view",
		scope: "both",
		bindings: [{ key: "Tab" }],
		label: kb_view_overview_label,
	},
	"view.fullscreen": {
		category: "view",
		scope: "presentation",
		bindings: [{ key: "f" }],
		label: kb_view_fullscreen_label,
	},
	"system.reset-timer": {
		category: "system",
		scope: "presenter",
		bindings: [{ key: "r" }],
		label: kb_system_reset_timer_label,
	},
	"system.help": {
		category: "system",
		scope: "both",
		bindings: [{ key: "?", shift: true }, { key: "F1" }],
		label: kb_system_help_label,
	},
};

// matchBinding / matchAction / humanizeKey はそのまま維持
```

- [ ] **Step 4: `KeybindingHelpDialog` で `def.label()` `def.hint?.()` を呼ぶように更新**

`src/components/KeybindingHelpDialog.tsx`:

`CATEGORY_LABELS` を関数参照に変更:
```tsx
import {
	kb_category_navigation,
	kb_category_tools,
	kb_category_view,
	kb_category_system,
} from "#src/paraglide/messages.js";

const CATEGORY_LABELS: Record<Category, () => string> = {
	navigation: kb_category_navigation,
	tools: kb_category_tools,
	view: kb_category_view,
	system: kb_category_system,
};
```

`{CATEGORY_LABELS[cat]}` → `{CATEGORY_LABELS[cat]()}`

`ShortcutRow` 内:
```tsx
<span className="text-fg">{def.label()}</span>
{def.hint && <span className="text-muted text-xs">{def.hint()}</span>}
```

- [ ] **Step 5: 既存の `keybindings.test.ts` を確認**

```bash
pnpm test src/lib/keybindings.test.ts
```

`label` を string と比較しているテストがあれば `def.label()` に変更。具体的に:

```bash
grep -n "label" src/lib/keybindings.test.ts
```

該当箇所を `def.label()` 呼び出しに修正。例えば:
- `expect(KEYBINDING_CATALOG["slide.next"].label).toBe("Next slide")` →
- `expect(KEYBINDING_CATALOG["slide.next"].label()).toMatch(/Next slide|次のスライド/)`

(locale が test 開始時にどちらでもよいよう regex に)

- [ ] **Step 6: 全テスト実行**

```bash
pnpm test
```

Expected: 全 pass。

- [ ] **Step 7: tsc & lint 確認**

```bash
pnpm tsc -b
pnpm lint
```

- [ ] **Step 8: コミット**

```bash
git add src/lib/keybindings.ts src/components/KeybindingHelpDialog.tsx messages/ src/lib/keybindings.test.ts
git commit -m "refactor(keybindings): label/hint を paraglide 関数参照に

KEYBINDING_CATALOG の label と hint を () => string に変更し、
KeybindingHelpDialog の CATEGORY_LABELS とともに paraglide messages 経由で解決。
en/ja の翻訳を messages/ に追加。"
```

---

## Task 11: メッセージカタログ全体構築 (主要 area)

**Files:**
- Modify: `messages/en.json`
- Modify: `messages/ja.json`

このタスクは Task 12 以降の置換で必要な全 message key を一度に追加する。Task 10 で追加した `kb_*` キーは保持。

- [ ] **Step 1: `messages/en.json` を以下の完全版に更新**

```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "smoke_test": "ok",

  "meta_title": "PDFPW — Precise PDF presenter",
  "meta_description": "Browser-based PDF presenter console (pdfpc-compatible). No install, no upload.",

  "header_help_aria": "Keyboard shortcuts",
  "header_github_aria": "GitHub",
  "header_licenses_aria": "Licenses",
  "header_lang_aria": "Switch language",

  "hero_eyebrow": "PRESENTER CONSOLE / 001",
  "hero_headline_line1": "Precise by",
  "hero_headline_line2": "default.",
  "hero_lead": "A browser-based presenter console. No install, no cloud upload. Your PDF stays on your device.",
  "hero_open_pdf": "Open PDF",
  "hero_open_with_fsa": "Open with File System Access",
  "hero_drop_label": "Drop a PDF here",
  "hero_drop_hint_browse": "or click to browse",

  "howitworks_eyebrow": "HOW IT WORKS",
  "howitworks_steps_count": "3 steps",
  "howitworks_step1_title": "Open a PDF in your browser",
  "howitworks_step1_body": "No install, no cloud upload. Your file stays on your device. pdfpc configuration files are supported.",
  "howitworks_step2_title": "Pop out the presentation",
  "howitworks_step2_body": "Two synchronized windows: a private presenter console with notes and timer, plus a public fullscreen display.",
  "howitworks_step3_title": "Present with confidence",
  "howitworks_step3_body": "Notes, timer, laser pointer, pen, blackout — all keyboard-driven. Stay in flow.",

  "library_eyebrow": "LIBRARY",
  "library_subtitle_with_count": "{count, plural, one {# file} other {# files}} · recent first",
  "library_subtitle_empty": "recent first",
  "library_history_toggle": "Save history",
  "library_help_sr": "Help",
  "library_help_title": "About history saving",
  "library_help_advanced_heading": "Advanced mode (recommended)",
  "library_help_advanced_label": "File link",
  "library_help_advanced_body": "Stores only a pointer to the file, so you always open the latest version.",
  "library_help_standard_heading": "Standard mode",
  "library_help_standard_label": "Snapshot",
  "library_help_standard_body": "Saves the file content into the browser. If you update the original file later, opening from history shows the saved snapshot, not the new version.",
  "library_help_note_overwrite": "* Files with the same name overwrite older history entries.",
  "library_help_note_off": "* Turning off \"Save history\" disables history in both modes.",
  "library_clear_all": "Clear all",
  "library_disabled_message": "History saving is disabled.",
  "library_empty_hint": "No recent files yet. Drop a PDF above to start.",
  "library_open_latest": "Open latest version",
  "library_open_snapshot": "Open saved snapshot",
  "library_delete_aria": "Delete from library",
  "library_fsa_indicator_aria": "File System Access",

  "mode_freeze": "Freeze",
  "mode_blackout": "Blackout",
  "mode_overview": "Overview",
  "mode_freeze_aria": "Freeze projection",
  "mode_blackout_aria": "Blackout projection",
  "mode_overview_aria": "Show overview",

  "presenter_status_loading_with_config": "Loading \"{file}\" with config \"{config}\"…",
  "presenter_status_loading": "Loading \"{file}\"…",
  "presenter_error_no_pdf": "Please select a PDF file.",
  "presenter_error_permission_denied": "Permission denied. Please grant access again.",
  "presenter_error_config_name_mismatch": "Config file name does not match the PDF (example.pdf ↔ example.pdfpc).",
  "presenter_error_config_permission": "Permission for the config file was denied.",
  "presenter_error_no_file_permission": "No permission to open the file.",
  "presenter_error_pdfpc_pairing": "pdfpc must have the same name as the PDF (e.g. example.pdf ↔ example.pdfpc).",
  "presenter_error_open_failed": "Could not open the file.",
  "presenter_missing_no_file_title": "No file specified",
  "presenter_missing_not_found_title": "The specified file was not found",
  "presenter_missing_back_message": "Go back to the home page and select a file again.",
  "presenter_back_home": "Back to home",
  "presenter_notes_eyebrow": "Notes",

  "presentation_missing_no_file_title": "No file specified",
  "presentation_missing_back_message": "Go back to the home page and select a file again.",
  "presentation_back_home": "Back to home",
  "presentation_pairing_failed": "Pairing failed. Reopen the presenter window or check that the same file name is open.",
  "presentation_config_load_failed": "Could not load the configuration. Confirm that the presenter window is open with the same file.",
  "presentation_pdf_load_failed": "Could not load the PDF file. Confirm that the presenter window is open with the same file.",
  "presentation_error_generic": "An error occurred",
  "presentation_error_unexpected": "An unexpected error occurred",
  "presentation_error_retry": "Reload and retry",
  "presentation_connecting": "Connecting…",
  "presentation_connecting_hint": "Confirm that the presenter window is open.",
  "presentation_menu_help_aria": "Keyboard shortcuts",
  "presentation_menu_fullscreen_enter": "Enter fullscreen",
  "presentation_menu_fullscreen_exit": "Exit fullscreen",

  "kb_help_title": "Keyboard Shortcuts",
  "kb_help_press_again_prefix": "Press",
  "kb_help_press_again_suffix_with_esc_prefix": "again or",
  "kb_help_press_again_suffix": "to close",
  "kb_help_scope_presenter_aria": "Presenter screen",
  "kb_help_scope_presentation_aria": "Presentation (audience) screen",

  "kb_hint_prefix": "Press",
  "kb_hint_suffix": "for keyboard shortcuts",
  "kb_hint_dismiss_aria": "Dismiss hint",

  "overview_close_sr": "Close",
  "overview_eyebrow": "OVERVIEW",
  "overview_slides_suffix": "slides",
  "overview_footer_help": "Tab: toggle · Esc: close · Click: select",

  "update_message": "A new version is available",
  "update_reload": "Reload",

  "license_label_license": "License",
  "license_label_author": "Author"
}
```

- [ ] **Step 2: `messages/ja.json` を以下の完全版に更新**

```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "smoke_test": "OK",

  "meta_title": "PDFPW — 精密設計のプレゼンタコンソール",
  "meta_description": "ブラウザベースの PDF プレゼンテーション・コンソール (pdfpc 互換)。インストール不要、アップロード不要。",

  "header_help_aria": "キーボードショートカット",
  "header_github_aria": "GitHub",
  "header_licenses_aria": "ライセンス",
  "header_lang_aria": "言語を切り替え",

  "hero_eyebrow": "PRESENTER CONSOLE / 001",
  "hero_headline_line1": "Precise by",
  "hero_headline_line2": "default.",
  "hero_lead": "ブラウザで動くプレゼンタコンソール。インストール・クラウドアップロード不要。PDF はあなたの端末から離れません。",
  "hero_open_pdf": "PDF を開く",
  "hero_open_with_fsa": "File System Access で開く",
  "hero_drop_label": "PDF をここにドロップ",
  "hero_drop_hint_browse": "またはクリックして選択",

  "howitworks_eyebrow": "HOW IT WORKS",
  "howitworks_steps_count": "3 ステップ",
  "howitworks_step1_title": "ブラウザで PDF を開く",
  "howitworks_step1_body": "インストール・クラウドアップロード不要。ファイルはあなたの端末上にとどまります。pdfpc 設定ファイルにも対応。",
  "howitworks_step2_title": "プレゼンテーション画面を開く",
  "howitworks_step2_body": "ノートとタイマー付きの発表者用コンソール、客席用フルスクリーン表示の 2 ウィンドウが同期します。",
  "howitworks_step3_title": "落ち着いて発表する",
  "howitworks_step3_body": "ノート・タイマー・レーザーポインタ・ペン・ブラックアウトをキーボードで操作。集中を切らしません。",

  "library_eyebrow": "LIBRARY",
  "library_subtitle_with_count": "{count} 件 · 新しい順",
  "library_subtitle_empty": "新しい順",
  "library_history_toggle": "履歴を保存",
  "library_help_sr": "ヘルプ",
  "library_help_title": "履歴の保存について",
  "library_help_advanced_heading": "高機能モード (推奨)",
  "library_help_advanced_label": "ファイルへのリンク",
  "library_help_advanced_body": "ファイルへのポインタのみを保存するため、常に最新のファイルを開くことができます。",
  "library_help_standard_heading": "標準モード",
  "library_help_standard_label": "スナップショット保存",
  "library_help_standard_body": "その時点のファイルそのものをブラウザ内に保存します。元のファイルを更新しても、履歴から開く際は保存時の状態となります。",
  "library_help_note_overwrite": "※同名のファイルを開いた場合、古い履歴は上書きされます。",
  "library_help_note_off": "※「履歴を保存」をオフにすると、どちらのモードでも履歴は残らなくなります。",
  "library_clear_all": "すべて消去",
  "library_disabled_message": "履歴の保存が無効になっています。",
  "library_empty_hint": "履歴がまだありません。上の領域に PDF をドロップして始めましょう。",
  "library_open_latest": "最新のファイルを開く",
  "library_open_snapshot": "開いた時点の状態を復元",
  "library_delete_aria": "ライブラリから削除",
  "library_fsa_indicator_aria": "File System Access",

  "mode_freeze": "投影固定",
  "mode_blackout": "投影停止",
  "mode_overview": "一覧表示",
  "mode_freeze_aria": "投影固定",
  "mode_blackout_aria": "投影停止",
  "mode_overview_aria": "一覧表示",

  "presenter_status_loading_with_config": "「{file}」と設定ファイル「{config}」を読み込み中…",
  "presenter_status_loading": "「{file}」を読み込み中…",
  "presenter_error_no_pdf": "PDF ファイルを選択してください。",
  "presenter_error_permission_denied": "権限が拒否されました。再度許可してください。",
  "presenter_error_config_name_mismatch": "設定ファイル名が PDF と一致しません (example.pdf ↔ example.pdfpc)。",
  "presenter_error_config_permission": "設定ファイルの権限が拒否されました。",
  "presenter_error_no_file_permission": "ファイルへの権限がありません。",
  "presenter_error_pdfpc_pairing": "pdfpc は PDF と同じ名前にしてください (例: example.pdf ↔ example.pdfpc)。",
  "presenter_error_open_failed": "ファイルを開けませんでした。",
  "presenter_missing_no_file_title": "ファイルが指定されていません",
  "presenter_missing_not_found_title": "指定されたファイルが見つかりません",
  "presenter_missing_back_message": "ホームに戻って再度ファイルを選択してください。",
  "presenter_back_home": "ホームへ戻る",
  "presenter_notes_eyebrow": "Notes",

  "presentation_missing_no_file_title": "ファイルが指定されていません",
  "presentation_missing_back_message": "ホームに戻って再度ファイルを選択してください。",
  "presentation_back_home": "ホームへ戻る",
  "presentation_pairing_failed": "ペアリングに失敗しました。プレゼンター画面を開き直すか、同名のファイルで開いているか確認してください。",
  "presentation_config_load_failed": "設定を読み込めませんでした。同名のファイルでプレゼンター画面を開いているか確認してください。",
  "presentation_pdf_load_failed": "PDF ファイルの読み込みに失敗しました。同名のファイルでプレゼンター画面を開いているか確認してください。",
  "presentation_error_generic": "エラーが発生しました",
  "presentation_error_unexpected": "予期しないエラーが発生しました",
  "presentation_error_retry": "リロードして再試行",
  "presentation_connecting": "接続中…",
  "presentation_connecting_hint": "プレゼンター画面を開いているか確認してください。",
  "presentation_menu_help_aria": "キーボードショートカット",
  "presentation_menu_fullscreen_enter": "フルスクリーン開始",
  "presentation_menu_fullscreen_exit": "フルスクリーン解除",

  "kb_help_title": "キーボードショートカット",
  "kb_help_press_again_prefix": "もう一度",
  "kb_help_press_again_suffix_with_esc_prefix": "を押すか",
  "kb_help_press_again_suffix": "で閉じます",
  "kb_help_scope_presenter_aria": "プレゼンター画面",
  "kb_help_scope_presentation_aria": "プレゼンテーション (客席) 画面",

  "kb_hint_prefix": "",
  "kb_hint_suffix": "を押すとキーボードショートカット一覧",
  "kb_hint_dismiss_aria": "ヒントを閉じる",

  "overview_close_sr": "閉じる",
  "overview_eyebrow": "OVERVIEW",
  "overview_slides_suffix": "件",
  "overview_footer_help": "Tab: 切替 · Esc: 閉じる · Click: 選択",

  "update_message": "新しいバージョンがあります",
  "update_reload": "再読み込み",

  "license_label_license": "ライセンス",
  "license_label_author": "作者"
}
```

- [ ] **Step 3: paraglide compile**

```bash
pnpm exec paraglide-js compile --project ./project.inlang --outdir ./src/paraglide
```

Expected: エラーなし。`src/paraglide/messages.js` に新しい関数が増える。

- [ ] **Step 4: tsc 確認**

```bash
pnpm tsc -b
```

paraglide compile 出力に対する型エラーがあれば fix。

- [ ] **Step 5: コミット**

```bash
git add messages/en.json messages/ja.json
git commit -m "feat(i18n): メッセージカタログを全 area で en/ja に追加

ヘッダー、Hero、Library、ModeForm、エラー、Presentation、KeybindingHelp、
UpdateToast、Overview、ライセンスページ等の全 ~150 メッセージを定義。"
```

---

## Task 12: Header の文字列を i18n 化

**Files:**
- Modify: `src/components/Header.tsx`

- [ ] **Step 1: `Header.tsx` の aria-label を message 関数化**

```tsx
import * as m from "#src/paraglide/messages.js";
// ... 既存 imports ...

// onHelpClick のボタン:
aria-label={m.header_help_aria()}

// GitHub リンク:
aria-label={m.header_github_aria()}

// Licenses リンク:
aria-label={m.header_licenses_aria()}
```

`LocaleSwitcher.tsx` 内の `aria-label="Switch language"` も同様に:
```tsx
aria-label={m.header_lang_aria()}
```

- [ ] **Step 2: tsc / lint / dev 確認**

```bash
pnpm tsc -b
pnpm lint
```

- [ ] **Step 3: コミット**

```bash
git add src/components/Header.tsx src/components/LocaleSwitcher.tsx
git commit -m "feat(i18n): Header と LocaleSwitcher の aria-label を翻訳"
```

---

## Task 13: HeroSection を i18n 化

**Files:**
- Modify: `src/routes/$locale/(main)/-index/HeroSection.tsx`

- [ ] **Step 1: `HeroSection.tsx` の文字列を全て置換**

```tsx
import * as m from "#src/paraglide/messages.js";
// ... 既存 imports ...

// 既存固定文字列を以下に置換:
// "PRESENTER CONSOLE / 001" → そのまま (ブランド表示なので英語固定でも可、ただし翻訳鍵経由で柔軟性確保)
<div className="...">
	{m.hero_eyebrow()}
</div>

<h1 className="...">
	{m.hero_headline_line1()}
	<br />
	{m.hero_headline_line2()}
</h1>

<p className="...">
	{m.hero_lead()}
</p>

<Button ...>
	<PlusIcon className="size-4" />
	{m.hero_open_pdf()}
</Button>

{supportsFSA && (
	<Button ...>
		{m.hero_open_with_fsa()}
	</Button>
)}

// drop label
<span className="text-[13px] font-medium text-accent">
	{m.hero_drop_label()}
</span>
<span className="font-mono text-[11px] text-muted">
	{m.hero_drop_hint_browse()} &middot; <kbd>⌘O</kbd>
</span>
```

- [ ] **Step 2: tsc / dev 確認**

```bash
pnpm tsc -b
```

- [ ] **Step 3: コミット**

```bash
git add src/routes/\$locale/\(main\)/-index/HeroSection.tsx
git commit -m "feat(i18n): HeroSection の文字列を翻訳"
```

---

## Task 14: HowItWorksSection を i18n 化

**Files:**
- Modify: `src/routes/$locale/(main)/-index/HowItWorksSection.tsx`

- [ ] **Step 1: `HowItWorksSection.tsx` を以下に置換**

`STEPS` 配列を関数経由で組み立てる:

```tsx
import { FileTextIcon, KeyboardIcon, MonitorPlayIcon } from "lucide-react";
import * as m from "#src/paraglide/messages.js";

interface Step {
	number: string;
	icon: React.ReactNode;
	title: string;
	body: string;
}

function getSteps(): Step[] {
	return [
		{
			number: "01",
			icon: <FileTextIcon className="size-5 text-accent" />,
			title: m.howitworks_step1_title(),
			body: m.howitworks_step1_body(),
		},
		{
			number: "02",
			icon: <MonitorPlayIcon className="size-5 text-accent" />,
			title: m.howitworks_step2_title(),
			body: m.howitworks_step2_body(),
		},
		{
			number: "03",
			icon: <KeyboardIcon className="size-5 text-accent" />,
			title: m.howitworks_step3_title(),
			body: m.howitworks_step3_body(),
		},
	];
}

export function HowItWorksSection() {
	const steps = getSteps();
	return (
		<section className="border-t border-border">
			<div className="container mx-auto max-w-6xl px-6 py-12">
				<div className="mb-6 flex items-baseline justify-between">
					<div className="font-mono text-[10px] uppercase tracking-[0.14em] text-muted">
						{m.howitworks_eyebrow()}
					</div>
					<div className="text-[11px] text-subtle">
						{m.howitworks_steps_count()}
					</div>
				</div>
				<div className="grid grid-cols-1 gap-4 md:grid-cols-3">
					{steps.map((step) => (
						<article
							key={step.number}
							className="flex flex-col rounded-lg border border-border bg-raised p-5"
						>
							<div className="mb-3 flex items-center justify-between">
								<span className="font-mono text-[11px] font-semibold text-accent">
									{step.number}
								</span>
								{step.icon}
							</div>
							<h3 className="mb-2 text-[15px] font-medium tracking-tight text-fg">
								{step.title}
							</h3>
							<p className="text-[13px] leading-[1.55] text-muted">
								{step.body}
							</p>
						</article>
					))}
				</div>
			</div>
		</section>
	);
}
```

- [ ] **Step 2: tsc 確認**

```bash
pnpm tsc -b
```

- [ ] **Step 3: コミット**

```bash
git add src/routes/\$locale/\(main\)/-index/HowItWorksSection.tsx
git commit -m "feat(i18n): HowItWorksSection を翻訳"
```

---

## Task 15: LibrarySection を i18n 化

**Files:**
- Modify: `src/routes/$locale/(main)/-index/LibrarySection.tsx`

- [ ] **Step 1: `LibrarySection.tsx` の文字列を全置換**

```tsx
import { CircleHelp, FileClock, FileSymlink, XIcon } from "lucide-react";
import { Button } from "#src/components/ui/button";
import {
	Dialog,
	DialogContent,
	DialogDescription,
	DialogHeader,
	DialogTitle,
	DialogTrigger,
} from "#src/components/ui/dialog";
import { Skeleton } from "#src/components/ui/skeleton";
import { Switch } from "#src/components/ui/switch";
import { formatDateTime } from "#src/lib/format.ts";
import type { RecentFile, Settings } from "#src/lib/recent-store";
import * as m from "#src/paraglide/messages.js";

// (LibrarySectionProps 等の型はそのまま)

export function LibrarySection({
	supportsFSA,
	recentFiles,
	settings,
	onToggleHistory,
	onClearRecent,
	onRecentClick,
	onDeleteRecent,
}: LibrarySectionProps) {
	return (
		<section className="container mx-auto max-w-6xl px-6 py-14">
			<div className="mb-6 flex items-center justify-between gap-4">
				<div>
					<div className="font-mono text-[10px] uppercase tracking-[0.14em] text-muted">
						{m.library_eyebrow()}
					</div>
					<div className="text-[11px] text-subtle mt-0.5">
						{recentFiles.length > 0
							? m.library_subtitle_with_count({ count: recentFiles.length })
							: m.library_subtitle_empty()}
					</div>
				</div>
				<div className="flex items-center gap-3">
					<div className="flex items-center gap-2 rounded-md border border-border bg-raised px-3 py-1.5">
						<Switch
							id="save-history"
							checked={settings.saveHistory}
							onCheckedChange={onToggleHistory}
						/>
						<label
							htmlFor="save-history"
							className="text-[11px] font-medium text-muted select-none"
						>
							{m.library_history_toggle()}
						</label>
					</div>
					<Dialog>
						<DialogTrigger asChild>
							<Button variant="ghost" size="icon-sm">
								<CircleHelp className="h-4 w-4" />
								<span className="sr-only">{m.library_help_sr()}</span>
							</Button>
						</DialogTrigger>
						<DialogContent>
							<DialogHeader>
								<DialogTitle>{m.library_help_title()}</DialogTitle>
								<DialogDescription className="space-y-4 pt-4 text-left">
									<p>
										<strong>{m.library_help_advanced_heading()}</strong>
										<br />
										<span className="flex items-center gap-1">
											<FileSymlink className="h-3 w-3" />
											{m.library_help_advanced_label()}
										</span>
										<br />
										{m.library_help_advanced_body()}
									</p>
									<p>
										<strong>{m.library_help_standard_heading()}</strong>
										<br />
										<span className="flex items-center gap-1">
											<FileClock className="h-3 w-3" />
											{m.library_help_standard_label()}
										</span>
										<br />
										{m.library_help_standard_body()}
									</p>
									<p className="text-xs text-muted">
										{m.library_help_note_overwrite()}
										<br />
										{m.library_help_note_off()}
									</p>
								</DialogDescription>
							</DialogHeader>
						</DialogContent>
					</Dialog>
					{supportsFSA && recentFiles.length > 0 ? (
						<Button
							type="button"
							onClick={onClearRecent}
							variant="ghost"
							size="sm"
						>
							{m.library_clear_all()}
						</Button>
					) : null}
				</div>
			</div>

			{!settings.saveHistory && recentFiles.length === 0 ? (
				<div className="rounded-lg border border-dashed border-border bg-raised/40 p-8 text-center text-[13px] text-muted">
					{m.library_disabled_message()}
				</div>
			) : recentFiles.length === 0 ? (
				<div className="rounded-lg border border-dashed border-border bg-raised/40 p-8 text-center text-[13px] text-muted">
					{m.library_empty_hint()}
				</div>
			) : (
				<ul className="grid grid-cols-1 gap-3 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
					{recentFiles.map((item) => (
						<li key={item.id}>
							<div className="group relative overflow-hidden rounded-lg border border-border bg-raised transition-colors hover:border-border-strong">
								<button
									type="button"
									onClick={() => void onRecentClick(item)}
									className="block w-full p-2.5 text-left"
									title={
										item.handle
											? m.library_open_latest()
											: m.library_open_snapshot()
									}
								>
									<div className="relative mb-2 aspect-[4/3] overflow-hidden rounded-md bg-gradient-to-br from-surface to-bg">
										<div className="absolute inset-0 flex items-center justify-center text-subtle">
											{item.handle ? (
												<FileSymlink className="size-6" />
											) : (
												<FileClock className="size-6" />
											)}
										</div>
										{item.handle && (
											<span
												role="img"
												aria-label={m.library_fsa_indicator_aria()}
												className="absolute bottom-1.5 right-1.5 size-1.5 rounded-full bg-accent"
											/>
										)}
									</div>
									<div className="truncate text-[12px] font-medium text-fg">
										{item.name}
									</div>
									<div className="mt-0.5 font-mono text-[10px] text-subtle">
										{formatDateTime(item.lastOpened, {
											dateStyle: "medium",
											timeStyle: "short",
										})}
									</div>
								</button>
								<Button
									type="button"
									onClick={() => void onDeleteRecent(item.id)}
									variant="ghost"
									size="icon-sm"
									className="absolute right-1.5 top-1.5 opacity-0 transition hover:text-danger group-hover:opacity-100"
									aria-label={m.library_delete_aria()}
								>
									<XIcon className="size-3.5" />
								</Button>
							</div>
						</li>
					))}
				</ul>
			)}
		</section>
	);
}

export function LibrarySectionLoading() {
	return (
		<section className="container mx-auto max-w-6xl px-6 py-14">
			<div className="mb-6 flex items-center justify-between gap-4">
				<div>
					<div className="font-mono text-[10px] uppercase tracking-[0.14em] text-muted">
						{m.library_eyebrow()}
					</div>
					<div className="text-[11px] text-subtle mt-0.5">
						{m.library_subtitle_empty()}
					</div>
				</div>
				<div className="flex items-center gap-3">
					<div className="flex items-center gap-2 rounded-md border border-border bg-raised px-3 py-1.5">
						<Skeleton className="h-5 w-9 rounded-full" />
						<Skeleton className="h-3 w-16" />
					</div>
					<Button variant="ghost" size="icon-sm" disabled>
						<CircleHelp className="h-4 w-4" />
					</Button>
				</div>
			</div>
			<ul className="grid grid-cols-1 gap-3 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
				{Array.from({ length: 4 }).map((_, index) => (
					<li
						key={`library-skeleton-${
							// biome-ignore lint/suspicious/noArrayIndexKey: skeleton placeholder
							index
						}`}
					>
						<div className="overflow-hidden rounded-lg border border-border bg-raised p-2.5">
							<Skeleton className="mb-2 aspect-[4/3] w-full rounded-md" />
							<Skeleton className="h-3 w-3/4" />
							<Skeleton className="mt-1 h-2.5 w-1/2" />
						</div>
					</li>
				))}
			</ul>
		</section>
	);
}
```

- [ ] **Step 2: tsc / lint 確認**

```bash
pnpm tsc -b
pnpm lint
```

- [ ] **Step 3: コミット**

```bash
git add src/routes/\$locale/\(main\)/-index/LibrarySection.tsx
git commit -m "feat(i18n): LibrarySection を翻訳

履歴トグル、ヘルプモーダル、空状態、各種 aria-label、日付表示を全て
paraglide messages 経由に置換。formatDateTime で locale 連動の表示に。"
```

---

## Task 16: ModeForm を i18n 化

**Files:**
- Modify: `src/routes/$locale/(main)/-presenter/ModeForm.tsx`

- [ ] **Step 1: `ModeForm.tsx` の日本語文字列を置換**

```tsx
import type { ClassValue } from "clsx";
import { Grid, MonitorOff, Snowflake } from "lucide-react";
import { Button } from "#src/components/ui/button.tsx";
import { Card, CardContent } from "#src/components/ui/card.tsx";
import { cn } from "#src/lib/utils.ts";
import * as m from "#src/paraglide/messages.js";

// ... 既存 props 型

export function ModeForm({
	className,
	isFrozen,
	onFrozenChange,
	isBlackout,
	onBlackoutChange,
	onOverviewModeOpen,
}: ModeFormProps) {
	return (
		<Card className={cn("py-3 @container", className)}>
			<CardContent className="px-3">
				<div className="flex gap-2">
					<Button
						type="button"
						variant={isFrozen ? "accent-ghost" : "ghost"}
						aria-pressed={isFrozen}
						className="flex-1 gap-2 h-9 min-w-0"
						size={"default"}
						onClick={() => onFrozenChange(!isFrozen)}
						aria-label={m.mode_freeze_aria()}
					>
						<Snowflake className="size-4 shrink-0" />
						<span className="hidden @[20rem]:inline">{m.mode_freeze()}</span>
					</Button>
					<Button
						type="button"
						variant={isBlackout ? "accent-ghost" : "ghost"}
						aria-pressed={isBlackout}
						className="flex-1 gap-2 h-9 min-w-0"
						size={"default"}
						onClick={() => onBlackoutChange(!isBlackout)}
						aria-label={m.mode_blackout_aria()}
					>
						<MonitorOff className="size-4 shrink-0" />
						<span className="hidden @[20rem]:inline">{m.mode_blackout()}</span>
					</Button>
					<Button
						type="button"
						variant="outline"
						className="flex-1 gap-2 h-9 min-w-0"
						size={"default"}
						onClick={onOverviewModeOpen}
						aria-label={m.mode_overview_aria()}
					>
						<Grid className="size-4 shrink-0" />
						<span className="hidden @[20rem]:inline">{m.mode_overview()}</span>
					</Button>
				</div>
			</CardContent>
		</Card>
	);
}
```

- [ ] **Step 2: tsc 確認**

```bash
pnpm tsc -b
```

- [ ] **Step 3: コミット**

```bash
git add src/routes/\$locale/\(main\)/-presenter/ModeForm.tsx
git commit -m "feat(i18n): ModeForm の \"投影固定/投影停止/一覧表示\" を翻訳"
```

---

## Task 17: Note と Timer の locale 連動

**Files:**
- Modify: `src/routes/$locale/(main)/-presenter/Note.tsx`
- Modify: `src/routes/$locale/(main)/-presenter/Timer.tsx`

- [ ] **Step 1: `Note.tsx` の "Notes" eyebrow を翻訳**

```tsx
import * as m from "#src/paraglide/messages.js";
// ...

// 既存:
// <div className="...">Notes</div>
// →
<div className="px-6 font-mono text-[10px] uppercase tracking-[0.14em] text-muted">
	{m.presenter_notes_eyebrow()}
</div>
```

- [ ] **Step 2: `Timer.tsx` の `Intl.DurationFormat` `Intl.DateTimeFormat` を locale 連動に変更**

`Timer.tsx` の上部 (既存のフォーマッタ作成部分) を以下に置換:

```tsx
import { getLocale } from "#src/paraglide/runtime.js";

// ... 既存 imports とその他

// 削除: const durationFormatter = new Intl.DurationFormat("ja-JP", { ... })
// 削除: const clockFormatter = new Intl.DateTimeFormat("ja-JP", { ... })

// 関数として locale 動的に生成:
function getDurationFormatter() {
	return new Intl.DurationFormat(getLocale(), {
		style: "digital",
		hours: "numeric",
		minutes: "2-digit",
		seconds: "2-digit",
	});
}
function getClockFormatter() {
	return new Intl.DateTimeFormat(getLocale(), {
		hour: "2-digit",
		minute: "2-digit",
	});
}

// 既存:
// function formatDuration(ms: number): string { ... return durationFormatter.format({...}); }
// →
function formatDuration(ms: number): string {
	const totalSeconds = Math.floor(Math.abs(ms) / 1000);
	const hours = Math.floor(totalSeconds / 3600);
	const minutes = Math.floor((totalSeconds % 3600) / 60);
	const seconds = totalSeconds % 60;
	return getDurationFormatter().format({
		hours: ms < 0 ? -hours : hours,
		minutes,
		seconds,
	});
}

// buildTimerView 内:
// 既存: clockText: clockFormatter.format(nowMs),
// →    clockText: getClockFormatter().format(nowMs),
```

- [ ] **Step 3: tsc / dev で確認**

```bash
pnpm tsc -b
```

ブラウザで `/en/presenter` `/ja/presenter` を切り替え (PDF 読み込み済み状態) で時計表示が locale 連動することを目視確認。

- [ ] **Step 4: コミット**

```bash
git add src/routes/\$locale/\(main\)/-presenter/Note.tsx src/routes/\$locale/\(main\)/-presenter/Timer.tsx
git commit -m "feat(i18n): Note の eyebrow と Timer フォーマッタを locale 連動に

Timer の DurationFormat と DateTimeFormat を ja-JP 固定から getLocale() に変更。"
```

---

## Task 18: Presenter route のエラー文言と status を i18n 化

**Files:**
- Modify: `src/routes/$locale/(main)/index.tsx`
- Modify: `src/routes/$locale/(main)/presenter.tsx`

- [ ] **Step 1: `(main)/index.tsx` のエラー status を翻訳**

`src/routes/$locale/(main)/index.tsx` 内の文字列を以下のように置換:

```tsx
import * as m from "#src/paraglide/messages.js";

// "PDFファイルを選択してください" → m.presenter_error_no_pdf()
// "「{pdf.name}」と設定ファイル「{pdfpc.name}」を読み込み中…" → m.presenter_status_loading_with_config({ file: pdf.name, config: pdfpc.name })
// "「{pdf.name}」を読み込み中…" → m.presenter_status_loading({ file: pdf.name })
// "権限が拒否されました。再度許可してください。" → m.presenter_error_permission_denied()
// "設定ファイル名がPDFと一致しません（example.pdf ↔ example.pdfpc）。" → m.presenter_error_config_name_mismatch()
// "設定ファイルの権限が拒否されました。" → m.presenter_error_config_permission()
// "ファイルへの権限がありません。" → m.presenter_error_no_file_permission()
// "pdfpc は PDF と同じ名前にしてください（例: example.pdf ↔ example.pdfpc）" → m.presenter_error_pdfpc_pairing()
// "ファイルを開けませんでした" → m.presenter_error_open_failed()
```

- [ ] **Step 2: `presenter.tsx` の MissingFileScreen を翻訳**

```tsx
import * as m from "#src/paraglide/messages.js";

function MissingFileScreen({ title }: { title: string }) {
	const { locale } = Route.useParams();
	return (
		<main className="min-h-screen bg-background text-foreground">
			<div className="mx-auto flex max-w-2xl flex-col gap-4 px-6 py-12">
				<h1 className="text-xl font-semibold">{title}</h1>
				<p className="text-muted-foreground">
					{m.presenter_missing_back_message()}
				</p>
				<Button asChild className="w-fit">
					<Link to="/$locale" params={{ locale }}>
						{m.presenter_back_home()}
					</Link>
				</Button>
			</div>
		</main>
	);
}

// 利用箇所:
// {!file && <MissingFileScreen title="ファイルが指定されていません" />}
// → {!file && <MissingFileScreen title={m.presenter_missing_no_file_title()} />}

// {!pdf && <MissingFileScreen title="指定されたファイルが見つかりません" />}
// → {!pdf && <MissingFileScreen title={m.presenter_missing_not_found_title()} />}
```

- [ ] **Step 3: tsc / dev 確認**

```bash
pnpm tsc -b
```

- [ ] **Step 4: コミット**

```bash
git add src/routes/\$locale/\(main\)/index.tsx src/routes/\$locale/\(main\)/presenter.tsx
git commit -m "feat(i18n): Presenter ルートのエラー status と MissingFileScreen を翻訳"
```

---

## Task 19: Presentation route と Menu を i18n 化

**Files:**
- Modify: `src/routes/$locale/presentation/index.tsx`
- Modify: `src/routes/$locale/presentation/-Menu.tsx`

- [ ] **Step 1: `presentation/-Menu.tsx` の aria-label を翻訳**

```tsx
import * as m from "#src/paraglide/messages.js";
// ...

// onHelpClick ボタン:
aria-label={m.presentation_menu_help_aria()}

// fullscreen ボタン:
aria-label={isFullscreen ? m.presentation_menu_fullscreen_exit() : m.presentation_menu_fullscreen_enter()}
```

- [ ] **Step 2: `presentation/index.tsx` のエラー画面と接続中表示を翻訳**

```tsx
import * as m from "#src/paraglide/messages.js";
// ...

// missing file ブランチ:
function RouteComponent() {
	const { file } = Route.useSearch({ select: ({ file }) => ({ file }) });
	const { locale } = Route.useParams();

	if (!file)
		return (
			<main className="min-h-screen bg-background text-foreground">
				<div className="mx-auto flex max-w-2xl flex-col gap-4 px-6 py-12">
					<h1 className="text-xl font-semibold">
						{m.presentation_missing_no_file_title()}
					</h1>
					<p className="text-muted-foreground">
						{m.presentation_missing_back_message()}
					</p>
					<Button asChild className="w-fit">
						<Link to="/$locale" params={{ locale }}>
							{m.presentation_back_home()}
						</Link>
					</Button>
				</div>
			</main>
		);

	return (
		<main className="min-h-screen grid bg-blackout">
			<ErrorBoundary
				fallbackRender={(error, reset) => {
					if (error instanceof Error) {
						switch (error.message) {
							case "TIMEOUT_PAIRING_PRESENTATION":
								return (
									<div className="h-full flex items-center justify-center">
										{m.presentation_pairing_failed()}
									</div>
								);
							case "TIMEOUT_LOADING_PDFPC_CONFIG":
								return (
									<div className="h-full flex items-center justify-center">
										{m.presentation_config_load_failed()}
									</div>
								);
							case "TIMEOUT_LOADING_PDF_BUFFER":
								return (
									<div className="h-full flex items-center justify-center">
										{m.presentation_pdf_load_failed()}
									</div>
								);
							default:
								return (
									<div className="h-full flex flex-col items-center justify-center gap-4 p-6">
										<div className="text-center">
											<div className="text-red-500 font-semibold mb-2">
												{m.presentation_error_generic()}
											</div>
											<div className="text-muted-foreground">{error.message}</div>
										</div>
										<button
											type="button"
											onClick={reset}
											className="px-4 py-2 bg-primary text-primary-foreground rounded-md hover:bg-primary/90"
										>
											{m.presentation_error_retry()}
										</button>
									</div>
								);
						}
					}
					return (
						<div className="h-full flex flex-col items-center justify-center gap-4 p-6">
							<div className="text-center">
								<div className="text-red-500 font-semibold mb-2">
									{m.presentation_error_unexpected()}
								</div>
							</div>
							<button
								type="button"
								onClick={reset}
								className="px-4 py-2 bg-primary text-primary-foreground rounded-md hover:bg-primary/90"
							>
								{m.presentation_error_retry()}
							</button>
						</div>
					);
				}}
			>
				<Suspense fallback={<Skeleton></Skeleton>}>
					<RecentPdfResolver fileName={file} />
				</Suspense>
			</ErrorBoundary>
		</main>
	);
}

// PresentationBroadcastData の loading state:
return (
	<div className="h-full flex items-center justify-center">
		<div className="text-center">
			<div className="text-lg mb-2">{m.presentation_connecting()}</div>
			<div className="text-sm text-muted-foreground">
				{m.presentation_connecting_hint()}
			</div>
		</div>
	</div>
);
```

- [ ] **Step 3: tsc / dev 確認**

```bash
pnpm tsc -b
```

- [ ] **Step 4: コミット**

```bash
git add src/routes/\$locale/presentation/
git commit -m "feat(i18n): Presentation route と Menu を翻訳"
```

---

## Task 20: KeybindingHelpDialog と KeybindingHintToast を i18n 化 (JSX 分割)

**Files:**
- Modify: `src/components/KeybindingHelpDialog.tsx`
- Modify: `src/components/KeybindingHintToast.tsx`

- [ ] **Step 1: `KeybindingHelpDialog.tsx` のタイトル、フッタ、scope badge aria-label を翻訳**

```tsx
import * as m from "#src/paraglide/messages.js";
// ... 既存 imports

// DialogTitle:
<DialogTitle>{m.kb_help_title()}</DialogTitle>

// フッタ "Press ? again or Esc to close" を分割翻訳:
<p className="text-muted text-xs text-center pt-2 border-t border-border">
	{m.kb_help_press_again_prefix()} <Kbd className="mx-1">?</Kbd>{" "}
	{m.kb_help_press_again_suffix_with_esc_prefix()}{" "}
	<Kbd className="mx-1">Esc</Kbd> {m.kb_help_press_again_suffix()}
</p>

// ScopeBadges 内 Badge の aria-label:
function Badge({ active, label }: { active: boolean; label: string }) {
	return (
		<span
			role="img"
			className={cn( /* ... */ )}
			aria-label={
				label === "P" ? m.kb_help_scope_presenter_aria() : m.kb_help_scope_presentation_aria()
			}
		>
			{label}
		</span>
	);
}
```

- [ ] **Step 2: `KeybindingHintToast.tsx` を JSX 分割形で翻訳**

```tsx
import { XIcon } from "lucide-react";
import { Button } from "#src/components/ui/button.tsx";
import { Kbd } from "#src/components/ui/kbd.tsx";
import * as m from "#src/paraglide/messages.js";

interface Props {
	visible: boolean;
	onDismiss: () => void;
}

export function KeybindingHintToast({ visible, onDismiss }: Props) {
	if (!visible) return null;

	return (
		<output className="fixed bottom-4 right-4 z-50 flex items-center gap-3 rounded-lg border border-border bg-overlay/95 backdrop-blur-md px-3 py-2 text-fg shadow-[var(--shadow-lg)]">
			<span className="text-sm">
				{m.kb_hint_prefix()} <Kbd className="mx-1">?</Kbd> {m.kb_hint_suffix()}
			</span>
			<Button
				variant="ghost"
				size="icon-sm"
				onClick={onDismiss}
				aria-label={m.kb_hint_dismiss_aria()}
			>
				<XIcon />
			</Button>
		</output>
	);
}
```

- [ ] **Step 3: tsc / lint / 既存テスト確認**

```bash
pnpm tsc -b
pnpm lint
pnpm test src/components/KeybindingHelpDialog
```

- [ ] **Step 4: コミット**

```bash
git add src/components/KeybindingHelpDialog.tsx src/components/KeybindingHintToast.tsx
git commit -m "feat(i18n): KeybindingHelpDialog と HintToast を翻訳

Press ? again or Esc to close 等の JSX 埋め込み文字列は prefix/suffix に
分割して translate。"
```

---

## Task 21: UpdateToast / OverviewDialog / Licenses を i18n 化

**Files:**
- Modify: `src/components/UpdateToast.tsx`
- Modify: `src/components/OverviewDialog.tsx`
- Modify: `src/routes/$locale/(main)/licenses.tsx`

- [ ] **Step 1: `UpdateToast.tsx` を翻訳**

```tsx
import { useAtomValue } from "jotai";
import { RefreshCwIcon } from "lucide-react";
import { applyUpdate, updateAvailableAtom } from "../lib/pwa";
import * as m from "#src/paraglide/messages.js";
import { Button } from "./ui/button";

export default function UpdateToast() {
	const updateAvailable = useAtomValue(updateAvailableAtom);
	if (!updateAvailable) return null;

	return (
		<div className="fixed bottom-4 right-4 z-50 flex items-center gap-3 rounded-lg bg-gray-800 text-white px-4 py-3 shadow-lg">
			<span className="text-sm">{m.update_message()}</span>
			<Button
				size="sm"
				variant="secondary"
				onClick={() => {
					applyUpdate();
				}}
			>
				<RefreshCwIcon />
				{m.update_reload()}
			</Button>
		</div>
	);
}
```

- [ ] **Step 2: `OverviewDialog.tsx` の文字列を翻訳**

```tsx
import * as m from "#src/paraglide/messages.js";
// ...

// DialogPrimitive.Title 内:
<DialogPrimitive.Title className="font-mono text-[11px] uppercase tracking-[0.14em] text-muted">
	{m.overview_eyebrow()} · {pdfpcConfig.totalOverlays} {m.overview_slides_suffix()}
</DialogPrimitive.Title>

// Close 内 sr-only:
<span className="sr-only">{m.overview_close_sr()}</span>

// SVG title:
<title>{m.overview_close_sr()}</title>

// フッタ "Tab: toggle · Esc: close · Click: select":
<div className="border-t border-border pt-4 mt-4 font-mono text-[11px] text-muted">
	{m.overview_footer_help()}
</div>
```

- [ ] **Step 3: `licenses.tsx` の "License" "Author" ラベルを翻訳**

```tsx
import * as m from "#src/paraglide/messages.js";
// ...

function LicenseCard({ license }: { license: License }) {
	return (
		<Card>
			<CardHeader>
				<CardTitle>{license.name}</CardTitle>
				<CardDescription>
					{m.license_label_license()}: {license.license}
					{license.author && (
						<>
							{" "}
							| {m.license_label_author()}: {license.author}
						</>
					)}
				</CardDescription>
			</CardHeader>
			<CardContent className="whitespace-pre-wrap wrap-break-word text-sm leading-relaxed text-justify">
				{license.licenseText}
			</CardContent>
		</Card>
	);
}
```

- [ ] **Step 4: tsc 確認**

```bash
pnpm tsc -b
```

- [ ] **Step 5: コミット**

```bash
git add src/components/UpdateToast.tsx src/components/OverviewDialog.tsx src/routes/\$locale/\(main\)/licenses.tsx
git commit -m "feat(i18n): UpdateToast / OverviewDialog / Licenses を翻訳"
```

---

## Task 22: Presenter→Presentation popup URL に locale 含める

**Files:**
- Modify: `src/routes/$locale/(main)/index.tsx`

このタスクは Task 7 で `router.buildLocation` を `$locale` 対応にした際に既に対応済みのはずだが、念のため確認する。

- [ ] **Step 1: `(main)/index.tsx` の `presentationWindow = window.open(url, ...)` 周辺を確認**

```bash
grep -n "presentationWindow\|buildLocation" src/routes/\$locale/\(main\)/index.tsx
```

`buildLocation({ to: "/$locale/presentation", params: { locale }, search: ... })` になっていることを確認。

- [ ] **Step 2: 動作確認**

```bash
pnpm dev
```

- `http://localhost:6123/ja` にアクセス
- PDF を読み込む
- popup として presentation ウィンドウが `/ja/presentation/...` で開くことを確認

`Ctrl-C` で停止。

- [ ] **Step 3: 必要であれば修正してコミット**

修正不要なら次のタスクへ。修正した場合:

```bash
git add src/routes/\$locale/\(main\)/index.tsx
git commit -m "fix(i18n): presenter から presentation popup を locale prefix 付きで開く"
```

---

## Task 23: index.template.html を作成

**Files:**
- Create: `index.template.html`

- [ ] **Step 1: `index.template.html` を作成**

```html
<!doctype html>
<html lang="%LANG%" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="theme-color" content="#000000" />
    <title>%TITLE%</title>
    <meta name="description" content="%DESCRIPTION%" />
    <link rel="canonical" href="https://pdfpw.github.io/%LOCALE%/" />
    <link rel="alternate" hreflang="en" href="https://pdfpw.github.io/en/" />
    <link rel="alternate" hreflang="ja" href="https://pdfpw.github.io/ja/" />
    <link rel="alternate" hreflang="x-default" href="https://pdfpw.github.io/en/" />
    <meta property="og:locale" content="%OG_LOCALE%" />
    <meta property="og:title" content="%TITLE%" />
    <meta property="og:description" content="%DESCRIPTION%" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://pdfpw.github.io/%LOCALE%/" />
    <link rel="apple-touch-icon" href="/logo192.png" />
    <script>
      (() => {
        var stored, isDark;
        try {
          stored = localStorage.getItem("pdfpw-theme");
          isDark = stored
            ? stored === "dark"
            : window.matchMedia("(prefers-color-scheme: dark)").matches;
          document.documentElement.classList.toggle("dark", isDark);
        } catch (_e) {}
      })();
    </script>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 2: コミット**

```bash
git add index.template.html
git commit -m "feat(i18n): locale 別 HTML 用テンプレートを追加

%LANG% %LOCALE% %TITLE% %DESCRIPTION% %OG_LOCALE% トークンを含み、
ビルド時に locale ごとに展開される。"
```

---

## Task 24: ルート `index.html` を検出 redirect ページに刷新

**Files:**
- Modify: `index.html`

- [ ] **Step 1: `index.html` を以下の最小 redirect ページに置換**

```html
<!doctype html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="theme-color" content="#000000" />
    <title>PDFPW</title>
    <link rel="canonical" href="https://pdfpw.github.io/en/" />
    <script>
      (() => {
        try {
          var stored = localStorage.getItem("pdfpw:locale");
          var locale = stored === "ja" || stored === "en" ? stored : null;
          if (!locale) locale = navigator.language && navigator.language.toLowerCase().startsWith("ja") ? "ja" : "en";
          if (location.pathname === "/" || location.pathname === "") {
            location.replace("/" + locale + "/" + location.search + location.hash);
          }
        } catch (_) {}
        try {
          var theme = localStorage.getItem("pdfpw-theme");
          var isDark = theme ? theme === "dark" : window.matchMedia("(prefers-color-scheme: dark)").matches;
          document.documentElement.classList.toggle("dark", isDark);
        } catch (_) {}
      })();
    </script>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 2: dev で確認**

```bash
pnpm dev
```

`http://localhost:6123/` にアクセス → 即座に `/en` か `/ja` にリダイレクトされる。`Ctrl-C` で停止。

- [ ] **Step 3: コミット**

```bash
git add index.html
git commit -m "feat(i18n): ルート index.html を locale 検出 redirect ページに

inline script で localStorage / navigator.language から locale を検出し、
即座に /en or /ja に location.replace。React アプリも fallback として動く。"
```

---

## Task 25: localeHtmlPlugin で multi-HTML を生成

**Files:**
- Create: `src/vite-plugins/locale-html-plugin.ts`
- Modify: `vite.config.ts`

- [ ] **Step 1: `src/vite-plugins/locale-html-plugin.ts` を作成**

```ts
import { readFile, mkdir, writeFile, copyFile, rm } from "node:fs/promises";
import { resolve } from "node:path";
import type { Plugin } from "vite";

interface LocaleConfig {
	locale: "en" | "ja";
	lang: string;
	ogLocale: string;
}

const LOCALE_CONFIGS: LocaleConfig[] = [
	{ locale: "en", lang: "en", ogLocale: "en_US" },
	{ locale: "ja", lang: "ja", ogLocale: "ja_JP" },
];

const META = {
	en: {
		title: "PDFPW — Precise PDF presenter",
		description:
			"Browser-based PDF presenter console (pdfpc-compatible). No install, no upload.",
	},
	ja: {
		title: "PDFPW — 精密設計のプレゼンタコンソール",
		description:
			"ブラウザベースの PDF プレゼンテーション・コンソール (pdfpc 互換)。インストール不要、アップロード不要。",
	},
} as const;

function renderTemplate(template: string, cfg: LocaleConfig): string {
	const meta = META[cfg.locale];
	return template
		.replaceAll("%LANG%", cfg.lang)
		.replaceAll("%LOCALE%", cfg.locale)
		.replaceAll("%TITLE%", escapeHtml(meta.title))
		.replaceAll("%DESCRIPTION%", escapeHtml(meta.description))
		.replaceAll("%OG_LOCALE%", cfg.ogLocale);
}

function escapeHtml(s: string): string {
	return s
		.replace(/&/g, "&amp;")
		.replace(/</g, "&lt;")
		.replace(/>/g, "&gt;")
		.replace(/"/g, "&quot;")
		.replace(/'/g, "&#39;");
}

export function localeHtmlPlugin(): Plugin {
	const root = process.cwd();
	let templatePath: string;
	const generatedDirs: string[] = [];

	return {
		name: "locale-html",
		async config() {
			templatePath = resolve(root, "index.template.html");
			const template = await readFile(templatePath, "utf-8");
			for (const cfg of LOCALE_CONFIGS) {
				const dir = resolve(root, cfg.locale);
				await mkdir(dir, { recursive: true });
				await writeFile(
					resolve(dir, "index.html"),
					renderTemplate(template, cfg),
					"utf-8",
				);
				generatedDirs.push(dir);
			}
			return {
				build: {
					rollupOptions: {
						input: {
							root: resolve(root, "index.html"),
							en: resolve(root, "en/index.html"),
							ja: resolve(root, "ja/index.html"),
						},
					},
				},
			};
		},
		async closeBundle() {
			// 生成ディレクトリを source 側からクリーンアップ
			for (const dir of generatedDirs) {
				await rm(dir, { recursive: true, force: true });
			}
		},
		async writeBundle(options) {
			// dist/index.html を 404.html にコピー (SPA fallback)
			const outDir = options.dir ?? resolve(root, "dist");
			const src = resolve(outDir, "index.html");
			const dest = resolve(outDir, "404.html");
			try {
				await copyFile(src, dest);
			} catch {
				// dist/index.html が無い場合 (rollup エラー等) はスキップ
			}
		},
	};
}
```

- [ ] **Step 2: `vite.config.ts` に plugin を追加**

```ts
import { localeHtmlPlugin } from "./src/vite-plugins/locale-html-plugin";
// ...

plugins: [
	paraglideVitePlugin({ /* ... */ }),
	localeHtmlPlugin(),
	UnpluginTypia(),
	// ... 既存
],
```

- [ ] **Step 3: ビルド実行**

```bash
pnpm build
```

Expected: `dist/index.html`, `dist/en/index.html`, `dist/ja/index.html`, `dist/404.html` が生成される。

- [ ] **Step 4: dist 確認**

```bash
ls dist/
ls dist/en/
ls dist/ja/
grep -o '<html lang="[^"]*"' dist/en/index.html dist/ja/index.html
grep -o '<title>[^<]*</title>' dist/en/index.html dist/ja/index.html
```

Expected:
- `dist/en/index.html` の `<html lang="en">` と英語 title
- `dist/ja/index.html` の `<html lang="ja">` と日本語 title
- `dist/404.html` が存在

- [ ] **Step 5: 一時生成された source 側 `en/` `ja/` ディレクトリが残っていないことを確認**

```bash
ls en/ ja/ 2>&1 | head -5
```

Expected: ディレクトリ無し (closeBundle で削除済み)。

- [ ] **Step 6: vite preview で動作確認**

```bash
pnpm preview
```

`http://localhost:4173/` にアクセス → `/en/` か `/ja/` にリダイレクト。`http://localhost:4173/ja/` 直アクセスで日本語タイトルがソースに含まれる。`Ctrl-C` で停止。

- [ ] **Step 7: コミット**

```bash
git add src/vite-plugins/locale-html-plugin.ts vite.config.ts
git commit -m "feat(i18n): localeHtmlPlugin で en/index.html ja/index.html を生成

ビルド時に index.template.html から locale 別の <head> を持つ HTML を生成し、
multi-input rollup で配信。dist/404.html は SPA fallback として index.html を
コピー。"
```

---

## Task 26: ビルド成果物のクリーン確認

**Files:** なし (検証のみ)

- [ ] **Step 1: クリーンビルド**

```bash
rm -rf dist
pnpm build
```

- [ ] **Step 2: 重要 HTML の検証**

```bash
echo "=== dist/index.html (root, redirect) ==="
grep -E "(canonical|location.replace|<title>)" dist/index.html | head -5

echo "=== dist/en/index.html ==="
grep -E "(<html lang|<title>|canonical|hreflang|og:locale)" dist/en/index.html | head -10

echo "=== dist/ja/index.html ==="
grep -E "(<html lang|<title>|canonical|hreflang|og:locale)" dist/ja/index.html | head -10

echo "=== dist/404.html (SPA fallback) ==="
grep -E "(location.replace|<title>)" dist/404.html | head -5
```

Expected:
- `en/index.html`: `<html lang="en">`, 英語 title, `og:locale en_US`, hreflang リンク 3 つ
- `ja/index.html`: `<html lang="ja">`, 日本語 title, `og:locale ja_JP`, hreflang リンク 3 つ
- `404.html`: redirect script を含む

- [ ] **Step 3: コミット (検証のみなので変更なし、次タスクへ)**

---

## Task 27: メッセージカタログ整合性テスト

**Files:**
- Create: `src/lib/i18n.test.ts`

- [ ] **Step 1: テストを書く**

`src/lib/i18n.test.ts`:

```ts
// @vitest-environment node
import { describe, expect, it } from "vitest";
import enMessages from "../../messages/en.json";
import jaMessages from "../../messages/ja.json";

const META_KEYS = new Set(["$schema"]);

function payloadKeys(json: Record<string, unknown>): string[] {
	return Object.keys(json)
		.filter((k) => !META_KEYS.has(k))
		.sort();
}

describe("messages catalog integrity", () => {
	it("en と ja で同一のキー集合を持つ", () => {
		const enKeys = payloadKeys(enMessages as Record<string, unknown>);
		const jaKeys = payloadKeys(jaMessages as Record<string, unknown>);
		expect(jaKeys).toEqual(enKeys);
	});

	it("値は全て non-empty な string", () => {
		for (const json of [enMessages, jaMessages] as const) {
			for (const [k, v] of Object.entries(json)) {
				if (META_KEYS.has(k)) continue;
				expect(typeof v, `${k} should be string`).toBe("string");
				if (k !== "kb_hint_prefix") {
					// kb_hint_prefix は ja で空になる場合があるので除外
					expect((v as string).length, `${k} should be non-empty`).toBeGreaterThan(
						0,
					);
				}
			}
		}
	});
});
```

- [ ] **Step 2: テスト実行**

```bash
pnpm test src/lib/i18n.test.ts
```

Expected: 2 tests passed。失敗した場合 (キー集合が不整合等) は messages/{en,ja}.json を修正。

- [ ] **Step 3: コミット**

```bash
git add src/lib/i18n.test.ts
git commit -m "test(i18n): メッセージカタログ整合性テスト追加

en と ja で同じキー集合を持つこと、値が non-empty string であることを確認。"
```

---

## Task 28: 最終検証

**Files:** なし (検証のみ)

- [ ] **Step 1: 全テスト実行**

```bash
pnpm test
```

Expected: 全 pass。

- [ ] **Step 2: tsc**

```bash
pnpm tsc -b
```

Expected: エラーなし。

- [ ] **Step 3: lint**

```bash
pnpm lint
```

Expected: エラーなし (warning のみ許容)。

- [ ] **Step 4: clean build**

```bash
rm -rf dist
pnpm build
```

Expected: 成功、`dist/{index,en/index,ja/index,404}.html` 全て生成。

- [ ] **Step 5: preview で手動動作確認**

```bash
pnpm preview
```

確認項目:
- `/` アクセス → 自動的に `/en` or `/ja` へ
- LocaleSwitcher で `EN ↔ JA` 切替時に URL prefix と画面言語が同時に変わる
- presenter / presentation 両画面で日英切替動作
- presenter から presentation popup を開いた際 URL が `/[locale]/presentation/...` になる
- KeybindingHelpDialog (`?` または F1) が開いて翻訳された label が表示される
- KeybindingHintToast の文言が locale で切り替わる
- Library のヘルプモーダルが日英で正しい説明を出す
- Timer の時計表示が locale 連動 (en で AM/PM、ja で 24h)

`Ctrl-C` で停止。

- [ ] **Step 6: コミットなし。検証完了**

ここまでで i18n 基盤の導入は完了。

---

## Self-Review

スペックの全項目を実装したかを確認。

- ✅ paraglide-js v2 + Vite plugin: Task 1, 2
- ✅ messages/{en,ja}.json: Task 1, 11
- ✅ src/paraglide/ 自動生成 + gitignore: Task 1
- ✅ Routing `$locale/` 配下に移動: Task 4
- ✅ `$locale/route.tsx` 検証 + paraglide 同期: Task 5
- ✅ ルート `/` redirect: Task 6
- ✅ 内部 Link / navigate 全 locale 化: Task 7
- ✅ LocaleSwitcher: Task 8, 9
- ✅ KEYBINDING_CATALOG 関数参照化: Task 10
- ✅ Header i18n: Task 12
- ✅ Hero / HowItWorks / Library / ModeForm: Task 13, 14, 15, 16
- ✅ Note / Timer (Intl locale): Task 17
- ✅ Presenter エラー / 状態: Task 18
- ✅ Presentation エラー / Menu: Task 19
- ✅ KeybindingHelpDialog / HintToast (JSX 分割): Task 20
- ✅ UpdateToast / Overview / Licenses: Task 21
- ✅ Presenter→Presentation popup URL: Task 22
- ✅ index.template.html: Task 23
- ✅ ルート index.html redirect: Task 24
- ✅ localeHtmlPlugin (multi-HTML, 404.html): Task 25
- ✅ メッセージ整合性テスト: Task 27
- ✅ 最終検証: Task 28

ギャップなし。
